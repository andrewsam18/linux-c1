<?
$msg="hii andrewsam";
$dif="differ";
echo"hello :".$msg,$dif;

print  "hi :$msg, $dif";
// PHP code demonstrating the use of echo and print
// echo() and print() are both used to output strings in PHP, but they have some differences.
// echo() can take multiple parameters, while print() can only take one.
// echo() does not return a value, while print() returns 1. 
// echo() is slightly faster than print() because it does not return a value.

 ?>

